const mediaControllers = require('./mediaControllers');
const userControllers = require('./userControllers');
const refreshTokenControllers = require('./refreshTokenControllers')

module.exports = {
    mediaControllers,
    userControllers,
    refreshTokenControllers
}